name: target_sistemas_prova
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter  
  cupertino_icons: ^1.0.2
  mobx: ^2.2.3
  flutter_mobx: ^2.2.0+1
  go_router: ^12.1.3
  get_it: ^7.6.4
  url_launcher: ^6.2.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.7
  mobx_codegen: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/mock/